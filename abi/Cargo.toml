[package]
name = "abi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
linera-sdk = "0.15.8"
async-graphql = "7.0"
serde = { version = "1.0", features = ["derive"] }

[features]
default = []