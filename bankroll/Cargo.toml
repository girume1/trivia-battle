[package]
name = "bankroll"
version = "0.1.0"
edition = "2021"

[lib]
name = "bankroll"
crate-type = ["cdylib", "rlib"]

[dependencies]
linera-sdk = "0.15.5"  # Match your abi version
serde = { version = "1.0", features = ["derive"] }
async-graphql = "7.0.0"
