{
  "installCommand": "if [ -d flutter ]; then cd flutter && git pull && cd ..; else git clone https://github.com/flutter/flutter.git; fi && ./flutter/bin/flutter config --enable-web && cd frontend && flutter pub get",
  "buildCommand": "flutter/bin/flutter build web --release",
  "outputDirectory": "frontend/build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
