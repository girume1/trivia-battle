name: trivia_battle_frontend
description: Real-time multiplayer trivia game built on Linera microchains
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'  # Fixes the SDK constraint error

dependencies:
  flutter:
    sdk: flutter
  graphql_flutter: ^5.2.0
  graphql: ^5.2.0
  provider: ^6.1.2
  cupertino_icons: ^1.0.8

  # Good UX packages
  fluttertoast: ^8.2.8          # Toasts for success/error messages
  intl: ^0.19.0                 # For timers/formatting later

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Add assets later if you have images/icons
  # assets:
  #   - assets/images/